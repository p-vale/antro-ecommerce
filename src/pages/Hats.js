import Footer from "../components/Footer"

const Hats = (data) => {
  return (
    <div>
    {console.log(data)}
      <h1>Hello from Hats</h1>
      <Footer />
    </div>
  );
};

export default Hats;